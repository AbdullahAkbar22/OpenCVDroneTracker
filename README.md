# OpenCVDroneTracker
Python script that uses the OpenCV library to track the flight of a CrazyFlie DIY drone, drawing a trajectory line as the drone flies. 
<br>Also includes custom-written functions to allow the drone to be flown from the command line.